os: linux
node_js: node
sudo: required
env: DISPLAY=':99.0'
dist: trusty
cache:
  directories:
    - node_modules
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
before_script:
  - sh -e /etc/init.d/xvfb start
script:
  - npm run integration-test
